إشعار عائم (Toast) — كبسولة تقفز من الأسفل. يعيش داخل `toast-host` المثبّت أسفل المنتصف.

```jsx
<div className="toast-host">
  <Toast kind="ok">تم الحفظ</Toast>
</div>
```

- بدون `kind`: داكن (معلومة). `ok` أخضر («تم حفظ توقّعك»)، `err` أحمر («حدث خطأ، حاول مجدداً»).
